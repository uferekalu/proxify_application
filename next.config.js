/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  env: {
    title: "Proxify Joblike Form",
    titleDescription: "This is an application for form similar to Proxify Job Form"
  }
}

module.exports = nextConfig
